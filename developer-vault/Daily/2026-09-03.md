---
type: development-log
date: 2026-09-03
visibility: public
projects:
  - Evelyn
  - STL Geometry Editing
tags:
  - dev/daily
---

# 2026-09-03

## [[Projects/Evelyn|Evelyn]]

### 결과

- 개인 Local Voice의 목표를 마지막 음성 뒤 300ms soft endpoint와 추가 500ms reopen으로 동결했다.
  grace 안 재입력은 같은 PCM·ASR session의 한 발화로 처리하며, 현재 500ms hard endpoint는 구현·검증
  전까지 유지한다.
- 300ms 시점의 계산은 side-effect-free prepare로 제한하고, authoritative final과 exact binding이
  일치할 때만 accepted user turn을 승격하도록 했다. 구현·테스트·실제 장치 검증은 후속 범위다.
- 단기 제품 목표를 단일 PC의 로컬 방송 MVP로 전환했다. 기존 Control Page·Live2D·Local TTS를
  재사용한 3턴 비공개 OBS 녹화를 첫 checkpoint로 두고, 마이크와 외부 플랫폼 연동은 분리했다.
- 대량 방송 채팅은 LLM에 건별 전달하지 않고 bounded user slot·중복 군집·RRF 뒤 답할 한 건 또는
  침묵으로 축약하며, 첫 플랫폼을 CHZZK 공식 read-only user session으로 동결했다. 구현·OAuth·live
  실행은 아직 하지 않았다.

### 재사용 가능한 배움

- 비동기 요청 취소는 이미 기록되거나 외부로 전달된 효과의 rollback이 아니다. 늦은 입력을 합치는
  최적화는 계산 단계와 commit 단계를 분리하고, 재입력에는 임시 계산만 폐기해야 한다.

### 프로젝트 근거

- [2026-09-03 worklog](obsidian://open?vault=docs&file=worklog%2F2026-09-03) — `docs/worklog/2026-09-03.md`
- `plan.md` P1-0/P1-0c/P1-1A, `docs/KOREAN_ASR_TARGET_ARCHITECTURE.md`, `docs/02_DECISIONS.md`

### 범위와 다음

- 현재 runtime 동작과 Discord·barge-in은 바뀌지 않았다. 후속 구현은 capture/ASR state와
  prepare/promote/abort 계약을 함께 닫고 source 회귀 뒤 별도 승인된 microphone에서 검증한다.
- 첫 방송 checkpoint는 공개 송출 없이 text→음성→Live2D 출력만 검증한다. 공개 전에는 read-only
  overlay와 privacy 경계, 캐릭터·복제 음성 사용권을 별도로 확인한다.
- CHZZK adapter는 채팅 조회 단일 scope와 연결 세대까지만 currentness로 인정한다. 공식 수신 API에
  없는 개별 삭제·AutoMod·replay를 보장하지 않으며 source 구현과 shadow는 다시 명시 승인받는다.

## STL 메시 형상 편집

### 결과

- 두 부품이 포함된 STL에서 양끝 걸쇠는 강체로 유지하고 내부 정점이 없는 직선 웹만 1.5배
  연장한 binary STL을 생성했다.

### 증거 범위

- `local artifact verification`: 저장 후 다시 열어 의도한 bounding box 변화, 두 연결 부품과 기존
  면 수 유지, solid·manifold·orientation·self-intersection·invalid-point 검사를 확인했다.
- `제외`: 원본·수정 STL, 파일명, 로컬 경로, 해시와 화면은 공개 Vault에 기록하지 않았다.
- `한계`: 출력 메시만 검증했으며 실제 출력물의 fit·하중은 확인하지 않았다.

### 재사용 가능한 배움

- 직선 압출 구간 안에 정점이 없다면 재메시나 불리언 없이 한쪽 끝 형상을 강체 평행이동하는 것이
  끝 형상과 위상을 보존하는 가장 작은 연장 방법이다. 저장 후 재로딩한 메시로 치수·연결성·방향과
  자기교차를 다시 검사해야 한다.

### 프로젝트 근거

- 비공개 Codex 로컬 작업 산출물이며 원본과 수정 모델은 공개 저장소에 포함하지 않았다.
