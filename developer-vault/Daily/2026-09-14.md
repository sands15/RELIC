---
date: 2026-09-14
visibility: public
tags:
  - dev/daily
---

# 2026-09-14

## 검색 태그 판정과 기억 권한 분리

- Evelyn의 격리 합성 실험에서 독립 검증 응답을 실제 검색 경로에 연결하고, 판정 변경·삭제·복구 시 오래된 태그가 재사용되지 않는 회귀를 확인했다. 의미가 맞다는 판정은 기억을 읽을 권한을 대신하지 않으며, 주입 응답으로 계약이 통과한 것과 실제 모델의 의미 정확도는 별도로 검증해야 한다. 프로젝트 근거: `plan.md`의 P44 B 수용표, `docs/worklog/2026-09-14.md`. 운영 적용이나 실제 모델 품질 개선을 주장하지 않는다.

- 후속 격리 실험 준비에서 검증 단계의 소유권 검사만으로는 앞선 생성 요청을 보호하지 못하는 누락을 재현했다. 생성·판정 공통 전송 경계에서 기존 레코드 검증을 재사용해 요청 전 차단하고 정상 경로를 회귀 확인했다. 권한 검사는 최종 결과뿐 아니라 원문이 처음 외부 단계로 넘어가기 전에 적용해야 한다. 프로젝트 근거: `plan.md`와 `docs/worklog/2026-09-14.md`의 P44 C 사전 감사. 합성·로컬 검사이며 실제 개인 데이터 유출이나 운영 적용을 뜻하지 않는다.

## 임베딩 회수율과 최종 검색 성공 분리

- Evelyn 합성 진단에서 임베딩이 정답을 상위 후보에 포함해도 무관한 결과와 정답 없는 질문을 함께 반환하면 최종 검색은 실패함을 확인했다. 임베딩 품질·색인 근사 손실·필터/보류 정책을 나누고, 작은 후보 집합의 높은 회수율을 실제 검색 정확도로 확대하지 않는다. 프로젝트 근거: `plan.md`의 KURE-v1 진단, `docs/worklog/2026-09-14.md`. 운영 채택이나 전체 지연 개선을 뜻하지 않는다.
- 후속 고정 필터 비교에서는 임베딩 교체가 순위를 바꿔도 최종 성공률은 같고 CPU 비용은 증가했다. 후보에서 제외된 기억의 회수와 후보 안의 순위 개선은 다른 실험이다. 요청별 인코딩 횟수를 검사해 숨은 질문 캐시로 비용이 빠지는 문제를 잡았으며, 문서 갱신이 겹친 지연도 전체 결과에 남겼다. 근거: 같은 프로젝트 worklog의 P46 비교; 일반 의미검색 성능이나 GPU 결과로 확대하지 않는다.

## 근거 부족과 검색 순위 실패 구분

- Evelyn 공개 한국어 검색 실험에서 개발 세트로만 고른 혼합 정책을 두 별도 평가 세트와 실제 조회 경로에 연결해 재현했다. 검색 가능한 원문이 없는 실패는 순위 최적화로 고칠 수 없으므로 기존 실패 분모를 보존했다. 공개 문단 검색의 통과를 개인 기억·답변 생성 정확도로 확대하지 않고, 임베딩 단독 시간과 전체 조회 시간을 분리했다. 근거: 프로젝트 `plan.md`의 P47–P49, `docs/worklog/2026-09-14.md`.
- 후속 실험에서 올바른 문단을 찾아도 본문 잘림이나 다음 단계의 길이 제한 때문에 근거가 사라질 수 있음을 확인했다. 원문과 출처를 보존하면서 기존 프롬프트 예산 안에 전달하고, 정답 문자열뿐 아니라 원래 근거 위치가 실제 출력에 남는지 검사했다. 읽기 전용 조회의 무쓰기와 캐시 포화 경계도 회귀로 고정했다. 근거: 같은 프로젝트 기록의 P50–P52. 기존 표본의 수리 재생이며 운영 적용이나 실제 생성 답변 품질 검증은 아니다.

- Evelyn의 후속 격리 검증에서는 실제 기억 제공 함수의 근거·삭제 좌표가 최종 Main 요청 문맥까지 이어지는지 확인했다. 입력 정규화 때문에 실험과 실제 요청이 어긋난 경우, 비교 검사를 완화하지 않고 실제 임베딩 전처리의 동등성을 검증한 뒤 재생했다. 조립된 문맥도 기억 수정·삭제 뒤에는 전달 직전 다시 차단되어야 한다. 근거: 프로젝트 `plan.md`의 P53, `docs/worklog/2026-09-14.md`. 실제 모델 답변이나 운영 배포를 검증한 것은 아니다.

- Evelyn 조회 프로파일에서 반복되는 문서 특징 변환을 찾아, 최신 원문·권한·삭제 검사는 유지한 채 동일 본문의 계산 결과만 제한적으로 재사용했다. 동일 품질 재검증과 교대 측정에서 평균·중앙값은 줄었지만 재계산 요청 때문에 p95 개선은 작았다. 캐시 평균 효과를 꼬리 지연이나 실제 모델 추론까지의 개선으로 확대하지 않는다. 근거: 프로젝트 `plan.md`의 P54, `docs/worklog/2026-09-14.md`. 격리 실험이며 운영 반영은 아니다.
- 후속 직접 계측으로 캐시 폐기 후 재계산을 확인하고, 함수 단위 대안 두 가지를 별도 비계측 비교했다. 한 대안은 효과가 작고 다른 대안은 더 느려 반영하지 않았다. 프로파일에서 큰 비용으로 보이는 부분도 실제 대안의 속도 향상을 보장하지 않으며, 안전한 폐기 규칙을 약화하는 대신 측정으로 후보를 탈락시킨다. 근거: 같은 프로젝트 `plan.md`와 worklog의 P55. 새로운 전체 조회 개선을 주장하지 않는다.
- 같은 요청 안에서 최신 원문을 다시 읽고 해시를 검사하되, 경로와 내용이 완전히 같은 해석 결과만 재사용하는 실험을 검증했다. 가변 반환값 분리·요청 종료 폐기·UTF-8 바이트 한도를 따로 시험했고, 같은 품질에서 작지만 반복된 조회 비용 감소를 관측했다. 안전성 검사와 순수 계산 재사용을 분리하되, 작은 성능 차이를 전체 서비스 개선으로 확대하지 않는다. 근거: 같은 프로젝트의 P56. 운영 반영이나 모델 추론 지연 검증은 아니다.

- Evelyn 실제 임베딩 검증에서는 저장 벡터 재생과 실제 GPU 추론을 구분해 호출 수·반환 벡터·전체 조회 비용을 확인했다. 계산 성공과 종료 정리 성공도 별개이며, 텐서 해제 뒤 남는 라이브러리 작업공간을 작은 재현으로 분리해 수정했다. 실행별 임시 식별자는 무조건 같게 만들지 않고 실제 권한·삭제 연결을 먼저 검사한 뒤 의미만 비교한다. 근거: 프로젝트 `plan.md`와 `docs/worklog/2026-09-14.md`의 P57. 같은 공개 표본 검증이며 개인 기억 품질·운영 배포를 뜻하지 않는다.

- 공개 단일 문단 검색을 통과한 후보를 기존 다중 기억·제외·별칭 사례에 적용하자 성공률이 크게 낮아졌다. 한 개만 반환하는 정책은 여러 근거를 요구하는 계약을 구조적으로 충족할 수 없고, 일반적인 요청 문구가 무관한 기록을 높은 순위로 올릴 수도 있었다. 같은 입력의 제한된 질문 정규화 대조도 전체 성공을 복구하지 못했다. 평가 계약이 다른 결과를 하나의 정확도로 합치거나 범용 적용의 근거로 삼을 수 없음을 확인했다. 프로젝트 근거: Evelyn `plan.md`와 `docs/worklog/2026-09-14.md`의 P58. 작은 재사용 합성 사례이며 실제 개인 데이터 실패율이나 운영 반영을 뜻하지 않는다.

- 질문 정리·반환 개수·본문 조건의 고정 8조합을 전부 비교하니 다중 반환과 조건 처리가 도움을 줬지만 인용·별칭 실패는 남았다. 문자 포함 여부는 실제 주제나 별칭의 근거가 아니므로, 실패한 의미 계약을 자료구조 속도 개선으로 대체할 수 없다. 전체 조합과 실패 분모를 보존하고 의미 근거 검증을 분리해야 한다. 프로젝트 근거: Evelyn `plan.md`와 `docs/worklog/2026-09-14.md`의 P59. 재사용 합성 사례의 점수계층 비교이며 실제 서비스 품질이나 최적성 증거는 아니다.

## 탄성 링 돌기의 대칭과 비교 조건

- 현재 탄성 링에서 돌기 곡면만 반지름선 기준으로 대칭화하고, 나머지 탄성팔·장착부 보존과 출력 형상을 검사했다. 팔 형상이 같아도 돌기가 넓어지면 골에서 남는 눌림이 달라지므로 대칭화와 회전 저항 감소를 같은 결과로 취급하지 않는다. 기하학적 검증과 실제 힘·소리·내구성 검증은 분리했다. 근거: Nintendo Switch detent 프로젝트 `outputs/plan.md`, `outputs/ring_v10_symmetric/verification.json`.
- 이어 한 방향의 진입 곡면만 수정하고 반대 곡면을 보존했다. 접촉 계산에서는 회전 간격만 촘촘히 하면 성긴 곡면 표본의 오차가 미분 지표에 나타날 수 있어, 곡면과 회전 표본을 함께 확인했다. 형상 비교 지표의 개선은 실제 회전력 개선을 보장하지 않는다. 근거: 같은 프로젝트 `outputs/ring_v11_ccw/verification.json`, `outputs/plan.md`.
- 비교용 세 가지 링에는 구분용 점을 추가하고 공통 결합부를 유지했다. 형상을 더 많이 바꾼 순서와 계산상 저항 감소 순서도 달라질 수 있으므로, 번호를 성능 순위로 표시하지 않았다. 근거: 같은 프로젝트 `outputs/ring_v12_ccw_compare/verification.json`, `outputs/plan.md`.
- C3 링의 사용감 확인을 받아 설계 조정을 마무리하고 최종 전체 모델로 정리했다. 원본 부품 수량과 출력판을 보존하면서 교체 부품을 반영하고, 전체3MF와 개별 부품 파일이 같은 형상인지 검사했다. 사용감 승인과 정량 성능·장기 내구성 검증은 구분했다. 근거: 같은 프로젝트 `outputs/final_C3/manifest.json`, `outputs/final_C3/verification.json`, `outputs/plan.md`.
- 전체 3MF는 XML과 파일 참조 검사를 통과해도 실제 슬라이서에서 열리지 않을 수 있었다. 부품 연결 태그의 네임스페이스 표기를 가져오기 프로그램의 실제 해석 방식과 맞추고, 형상 보존 검사와 Bambu Studio 직접 열기를 함께 확인했다. 파일 무결성과 대상 앱 호환성을 별도 검증해야 한다. 근거: Nintendo Switch 디텐트 프로젝트 `outputs/final_C3/load_verification.json`, `outputs/plan.md`.
- 케이스 각인을 교체하면서 기존 조립부와 장식을 보존하고, 첨부 실루엣의 구멍·방향을 실제 출력 메시와 비교했다. 평평한 외벽의 공칭 두께가 안쪽 결합 홈 부근의 최소 두께를 대표하지 않으므로, 각인 뒤에 남는 연속 벽을 별도로 확인했다. 근거: Nintendo Switch 디텐트 프로젝트 `outputs/case_switch2/verification.json`, `outputs/case_switch2/case_review_geometry.json`, `outputs/plan.md`. 실제 출력 시험과는 구분한다.
